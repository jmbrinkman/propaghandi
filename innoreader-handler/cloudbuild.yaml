steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
  - 'functions'
  - 'deploy'
  - 'innoreader-handler'
  - '--gen2'
  - '--no-allow-unauthenticated'
  - '--service-account=innoreader-handler-sa@${PROJECT_ID}.iam.gserviceaccount.com'
  - '--build-service-account=projects/{PROJECT_ID}/serviceaccounts/cloud-build-sa'
  - '--region=$LOCATION'
  - '--source=$TRIGGER_NAME'
  - '--trigger-http'
  - '--runtime=python312'
  - '--entry-point=innoreader_handler'
  - '--memory=512M'
  - '--concurrency=5'
  - '--max-instances=10'
  - '--cpu=1'
  - '--timeout=360'
  - '--set-secrets'
  - 'POSTS_BUKET_NAME=posts_buckets_name:latest'

options:
  logging: CLOUD_LOGGING_ONLY
